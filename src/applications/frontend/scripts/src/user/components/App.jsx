import React from 'react';

import UserContainer from '../containers/UserContainer';
import Layout from 'Layout';

const app = () => (
    <Layout>
        <UserContainer />
    </Layout>
);

export default app;