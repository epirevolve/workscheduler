import * as actionTypes from '../actionTypes';

const ui = (state = {}, action) => {
    const payload = action.payload;
    switch (action.type) {
        default:
            return state;
    }
};

export default ui;